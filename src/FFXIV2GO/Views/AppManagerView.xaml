<UserControl x:Class="FFXIV2GO.Views.AppManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:loc="clr-namespace:FFXIV2GO.Services">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="SemiBold" />
            <TextBlock Text="{Binding Description}" FontSize="13" Opacity="0.7" Margin="0,6,0,0" TextWrapping="Wrap" />
            <ui:Button Content="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[AppManager.Refresh]}"
                       Command="{Binding RefreshCommand}"
                       HorizontalAlignment="Left"
                       Margin="0,12,0,0" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="0,16,0,0" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource ApplicationBackgroundBrush}"
                                CornerRadius="8"
                                Padding="16,10"
                                Margin="0,4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0">
                                    <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Status}" FontSize="11" Opacity="0.7" Margin="0,3,0,0" TextWrapping="Wrap" />
                                    <ProgressBar Height="3"
                                                 Minimum="0" Maximum="100"
                                                 Value="{Binding Progress}"
                                                 IsIndeterminate="{Binding IsBusy}"
                                                 Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisible}}"
                                                 Margin="0,6,0,0" />
                                </StackPanel>

                                <ui:Button Grid.Column="1"
                                           Content="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[AppManager.Uninstall]}"
                                           IsEnabled="{Binding CanUninstall}"
                                           Command="{Binding DataContext.UninstallCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                           CommandParameter="{Binding}"
                                           Margin="0,0,12,0" />

                                <ui:Button Grid.Column="2"
                                           Appearance="Primary"
                                           Content="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[AppManager.Install]}"
                                           IsEnabled="{Binding CanInstall}"
                                           Command="{Binding DataContext.InstallCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                           CommandParameter="{Binding}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
